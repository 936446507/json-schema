import { ValidateResult } from './interface';
declare function checkPropertyMultipleOf(key: string, multipleOf: number, value: number, message?: string): ValidateResult[];
export { checkPropertyMultipleOf };
